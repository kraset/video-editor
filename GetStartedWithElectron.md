## Starta Development server lokalt
`Set-ExecutionPolicy RemoteSigned -Scope Process`

# Create new Electron Typescript project
1. `npm init electron-app@latest my-ts-app -- --template=vite-typescript`
2. Go to folder `cd my-ts-app`

# Start the project
 `npm start`

# Build the distributable
`npm run make`
